# Shared default
VITE_USE_DEMO_DATA=false

# Development (`npm run dev`)
VITE_PROXY_TARGET=http://localhost:8080

# Production build (`npm run build`)
# Leave blank when CloudFront routes `/api` to the backend.
VITE_API_BASE_URL=
